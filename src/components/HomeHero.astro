---
/**
 * Homepage hero — full-bleed imagery, overlay, primary H1, CTA.
 * Background asset: /public/images/hero-interior.jpg
 */
---

<section class="home-hero" aria-labelledby="hero-heading">
	<div class="home-hero__inner">
		<h1 id="hero-heading" class="home-hero__title type-h1-72-700">
			<span class="home-hero__title-line">We bring your</span>
			<span class="home-hero__title-line">
				<span class="home-hero__title-accent">renovation ideas</span>
			</span>
			<span class="home-hero__title-line">to life.</span>
		</h1>
		<p class="home-hero__sub type-large-body-18-400">
			By understanding your renovation needs. We make it happen.
		</p>
		<a class="home-hero__cta type-button-16-700" href="/contact/#discuss">Discuss Your Project</a>
	</div>
</section>

<style>
	.home-hero {
		position: relative;
		min-height: min(88vh, 52rem);
		display: flex;
		align-items: center;
		background-color: var(--color-dark-gray);
		/* Uniform dark wash + angled ramp so copy stays legible on bright glazing (right). */
		background-image:
			linear-gradient(rgba(0, 0, 0, 0.42), rgba(0, 0, 0, 0.42)),
			linear-gradient(
				105deg,
				rgba(0, 0, 0, 0.38) 0%,
				rgba(0, 0, 0, 0.22) 48%,
				rgba(0, 0, 0, 0.52) 100%
			),
			url('/images/hero-interior.jpg');
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
	}

	.home-hero__inner {
		position: relative;
		z-index: 1;
		width: 100%;
		max-width: 52rem;
		padding: clamp(2.5rem, 8vh, 5rem) clamp(1.25rem, 10vw, 8rem);
	}

	.home-hero__title {
		margin: 0 0 1.125rem;
		color: var(--color-white);
		text-shadow:
			0 1px 2px rgba(0, 0, 0, 0.45),
			0 4px 28px rgba(0, 0, 0, 0.35);
	}

	.home-hero__title-line {
		display: block;
	}

	.home-hero__title-accent {
		color: var(--color-dark-khaki);
		text-shadow:
			0 1px 2px rgba(0, 0, 0, 0.5),
			0 3px 20px rgba(0, 0, 0, 0.4);
	}

	.home-hero__sub {
		margin: 0 0 1.75rem;
		max-width: 28rem;
		text-align: left;
		color: rgba(255, 255, 255, 0.95);
	}

	.home-hero__cta {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.85rem 1.5rem;
		text-decoration: none;
		color: var(--color-white);
		background: var(--color-dark-khaki);
		border-radius: 3px;
		transition: filter 0.2s ease, transform 0.2s ease;
	}

	.home-hero__cta:hover {
		filter: brightness(1.06);
	}

	.home-hero__cta:focus-visible {
		outline: 2px solid var(--color-white);
		outline-offset: 3px;
	}
</style>
