// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// Set to your production origin for correct canonical URLs and OG `og:url` in builds.
export default defineConfig({
	site: 'https://keystonerenovations.co.uk',
});
